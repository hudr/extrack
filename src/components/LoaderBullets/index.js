import React, { Component } from 'react'

import { StyledContainer, Title } from './styled'

export default class LoaderBullets extends Component {
  render() {
    return (
      <StyledContainer>
        <Title>Loading</Title>
      </StyledContainer>
    )
  }
}
